imports:
    - php

build:
    tests:
        override:
            -
                command: 'make phpunit'
                coverage:
                    file: 'build/coverage.clover'
                    format: 'php-clover'
